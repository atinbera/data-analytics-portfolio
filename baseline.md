# Baseline Skills 

## SQL
- Can do SELECT, WHERE, GROUP BY, basic JOINs.
- Struggle with window functions, CTEs.

**Example Query**
```
SELECT department, COUNT(*) 
FROM employees 
GROUP BY department;
```
